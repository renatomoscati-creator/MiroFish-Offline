# ===========================================
# MiroFish Backend Dependencies
# ===========================================
# Python 3.11+ supported. Python 3.12/3.13: see OASIS section below.
# Install: pip install -r requirements.txt
# ===========================================

# ============= 核心框架 =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= LLM 相关 =============
# OpenAI SDK（统一使用 OpenAI 格式调用 LLM / Ollama）
openai>=1.0.0
# HTTP client for Ollama embedding API
requests>=2.28.0

# ============= Neo4j Graph Database =============
neo4j>=5.15.0

# ============= OASIS 社交媒体模拟 =============
# camel-ai 0.2.79+ supports Python 3.12/3.13 without restriction.
# camel-oasis 0.2.5 has a metadata pin of <3.12 but the wheel is pure-Python
# and works on 3.12/3.13. Install with:
#   pip install camel-ai>=0.2.79
#   pip install --no-deps --ignore-requires-python camel-oasis==0.2.5
#   pip install igraph cairocffi sentence-transformers
# The lines below work on Python <=3.11 via normal pip install:
camel-ai>=0.2.79
# camel-oasis manual install needed on Python 3.12+ (see above)
igraph>=1.0.0
cairocffi>=1.7.0
sentence-transformers>=3.0.0

# ============= 文件处理 =============
PyMuPDF>=1.24.0
# 编码检测（支持非UTF-8编码的文本文件）
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= 工具库 =============
# 环境变量加载
python-dotenv>=1.0.0

# 数据验证
pydantic>=2.0.0
